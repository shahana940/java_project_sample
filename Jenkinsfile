pipeline{
    agent none
    stages{
        stage("build"){
            echo "this is build block"
        }

        stage("test"){
            echo "this is test block"
        }

        stage("deploy"){
            echo "this is deploy block"
        }
    }
}